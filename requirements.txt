# ---------------------------------------------------------------------------
# FinSight AI dependencies (versions follow the BRD operating environment).
# Phase 1 (schema) only strictly needs the database layer; later phases add
# their own libraries. Kept together here as the project backbone.
# ---------------------------------------------------------------------------

# --- Database layer (Phase 1: schema) ---
SQLAlchemy>=2.0,<3.0
python-dotenv>=1.0
psycopg2-binary>=2.9        # Postgres driver (production only; SQLite needs no driver)

# --- AI / model service (Phase 3) ---
transformers>=4.40,<5.0
torch>=2.0
# spacy>=3.7                 # deferred — see README "Scope decisions"
# en-core-web-trf

# --- Ingestion (Phase 2) ---
requests>=2.31
# praw>=7.7                  # Reddit API (added when the Reddit fetcher is built)

# --- Pipeline scheduling ---
# APScheduler>=3.10,<4.0

# --- Data processing & viz ---
# pandas>=2.0
# numpy>=1.26
# plotly>=5.0
# altair>=5.0

# --- Dashboard (Phase 4) ---
# streamlit>=1.30,<2.0

# --- Evaluation (Phase 6) ---
# scikit-learn>=1.3          # F1, confusion matrix, relevance classifier

streamlit>=1.30,<2.0
plotly>=5.0
pandas>=2.0
